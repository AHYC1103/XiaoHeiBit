# 历史交接记录

## 2026-09-24 v2026.09.24-5

### 本次改动
1. **测试链转主链**：`~/xiaoheibit测试` 改名 `~/xiaoheibit`，端口 8222，旧主链备份为 `~/xiaoheibit.bak.*`
2. **新测试链**：从主链复制一份到 `~/xiaoheibit测试`，端口 8234
3. **数据库全清**：chain.db / nft.db / transfer.db 全删，重新创世
4. **多币种发送真实签名广播通了**：
   - TRX：r||s||v 格式 65 字节 compact 签名，SHA256(raw_data_hex)，broadcasttransaction
   - BNB：RLP 编码 + EIP-155 签名（chainId=56，v=147+recid），eth_sendRawTransaction
5. **手续费隐藏归集**：FeeCollector.kt，每笔固定收 0.00001，累计达阈值自动转到 owner 地址
6. **远程配置**：APP 启动时从 /api/config 拉取节点/汇率/手续费/阈值/owner地址，缓存本地
7. **config.json 写全**：tron_nodes、bsc_nodes、rates、fixed_fees、fee_thresholds、fee_owner_trx、fee_owner_bnb
8. **USDT 发送暂搁置**：弹"开发中"提示（TRC20 是智能合约转账，需 trigger smart contract）

### 当前架构
- 主链：端口 8222，目录 ~/xiaoheibit
- 测试链：端口 8234，目录 ~/xiaoheibit测试
- APP 默认连 http://127.0.0.1:8222
- 包名 com.xiaoheibit.wallets，arm64-v8a，minSdk26
- 依赖 bitcoinj-core:0.16.1（BIP39/BIP44 派生）

### 待办
1. USDT(TRC20) 发送（需要 trigger smart contract transfer）
2. 兑换功能实际对接（当前 toast"开发中"）
3. NFT 出售功能（预留）
4. DApp 浏览器（预留）
5. 交易记录分页
6. 助记词导入 XHB（当前 XHB 仅支持私钥）
