# XiaoHeiBit 历史交接文档

## 项目概述
XiaoHeiBit（小黑币，符号 XHB）是一条从零手写的本地私有区块链，配套纯原生安卓钱包 APP。后端 Python Flask 仅 503KB，全项目不足 1MB。架构经历了从"服务端托管私钥 + WebUI"到"APP 本地签名 + 纯 API 后端"的演进，当前为 v2026.09.23 版本。

## 技术栈
- **后端**：Python 3 + Flask，手写 PoW 共识、secp256k1 签名验签、UTXO 模型。chain.db 已拆分为 blocks/transfer/nft 三个库（core/__init__.py 中 ATTACH）。端口主网 8222，测试网 8234。
- **APP**：Kotlin 原生（非 WebView），minSdk 26，arm64-v8a。零 Web 框架，所有 UI 代码手写 LinearLayout。依赖仅 bitcoinj-core 0.16.1（BIP39/BIP44）、zxing（扫码）、swiperefreshlayout。
- **加密**：XHB 用手写 secp256k1（零依赖）；多币种用 bitcoinj 派生 + bouncycastle KeccakDigest 做地址编码。

## 核心功能
1. **钱包**：创建/导入/导出/重命名/删除，私钥与配置存 APP 私有目录（/data/data/com.xiaoheibit.wallets/files），服务端不接触私钥。单币种（仅 XHB 私钥）与多币种（助记词派生）双模式。
2. **多币种**：XHB（m/44'/0'）、TRX（m/44'/195'）、BNB（m/44'/60'）、USDT（TRC20，同 TRX 路径）。BIP44 派生地址已验证与 Gem Wallet 一致。余额查 trongrid API（TRX）与 BSC RPC（BNB），30 秒轮询，查询失败保留旧值。
3. **交易**：XHB 本地签名后通过 transfer_signed 接口广播，服务端验签。发送其他币种 UI 已完成（含扫码、手续费￥显示、网络警告），公链签名广播待对接。
4. **NFT**：列表/详情/铸造/发送/接收，二维码格式 `XiaoHeiBit$Nft$随机码$时间戳`，一次性使用。出售功能预留。IPFS 图片用公开网关 `https://ipfs.thegraph.com/ipfs/` 预览。
5. **兑换**：XHB↔TRX/BNB/USDT UI 完成，汇率硬编码（XHB=1￥, TRX≈2.31, BNB≈5344, USDT≈6.72），实际链上兑换待对接。
6. **购买**：法币购买 XHB UI，CNY￥计价，汇率走 Rates。

## 已知问题与待办
- 发送其他币种仅 UI，未对接公链签名广播。
- 兑换功能仅 UI，确认按钮 toast"开发中"。
- USDT(TRC20) 余额查询框架已搭，实际逻辑返回 0。
- 手续费 +5-7% 隐藏归集逻辑未实现。
- 扫码登录（10 分钟有效期 + 设备信息）、自绘密码锁、主题切换预留未做。
- 汇率硬编码，后续可接实时 API。

## 部署与构建
- 构建：`gradle assembleRelease`，产物 `app/build/outputs/apk/release/app-arm64-v8a-release.apk`。
- 上传：`/tmp/up.py` 推到测试网 `~/xiaoheibit测试/tmp/XiaoHeiBit-local-wallet.apk`。
- SSH：`cn-hk-bgp-4.ofalias.net:50044`，用户 `u0_a298`。
- 主网 `~/xiaoheibit/`（端口 8222，不动），测试网 `~/xiaoheibit测试/`（端口 8234）。
- 启动脚本已优化，配置文件 config.json 有注释。区块奖励 10（在 core 共识代码中修改）。

## 版本号规则
versionCode 递增，versionName 格式 `vYYYY.MM.DD-N`（当天第 N 次修改）。每次改完必须更新版本号。
