# XiaoHeiBit 历史交接文档

## 项目概述
XiaoHeiBit（小黑币，符号 XHB）是一条从零手写的本地私有区块链，配套纯原生安卓钱包 APP。后端 Python Flask 仅 503KB，全项目不足 1MB。当前版本 v2026.09.24-22。

## 技术栈
- **后端**：Python 3 + Flask，手写 PoW 共识、secp256k1 签名验签、UTXO 模型。chain.db 拆分为 blocks/transfer/nft 三个库。主网 8222，测试网 8234。
- **APP**：Kotlin 原生（非 WebView），minSdk 26，arm64-v8a。依赖仅 bitcoinj-core 0.16.1、zxing。
- **公网**：frp 隧道 cn-hk-bgp-4.ofalias.net:18222（openfrp），SSH 52221。

## 核心功能
1. **钱包**：创建/导入/导出/重命名/删除，私钥存 APP 私有目录。单币种与多币种双模式。
2. **多币种**：XHB、TRX、BNB、USDT(TRC20)。BIP44 派生地址已验证与 Gem Wallet 一致。
3. **交易**：XHB 本地签名广播；TRX 真实签名（r||s||v 65字节 compact）；BNB 真实签名（RLP+EIP-155）；USDT 发送暂未实现。
4. **NFT**：列表/详情/铸造/发送/接收，二维码 XiaoHeiBit$Nft$随机码$时间戳。出售预留。
5. **手续费归集**：每笔固定 0.00001，累计达阈值自动转 owner 地址，配置从 /api/config 读取。
6. **远程配置**：APP 启动拉取 /api/config。
7. **引导页**：首次启动红底国旗+免责声明。
8. **网络切换**：公开主链（默认）/主网/测试网/自定义节点。

## 部署
- 主链运行在树莓派（Arch Linux，192.168.1.91），systemd user service 自启
- xiaoheibit.service + frpc.service
- Python 依赖装在 venv：flask、mnemonic、ecdsa、requests
- 手机 Termux 仅跑测试网

## 关键修复
- TRX 签名：v在前被拒，DER被拒，最终 r||s||v 65字节 compact 成功
- BSC 公钥坐标越界：toByteArray() 返回33字节，截取最后32字节
- 数据库外键 bug：去掉外键约束
- 优雅停止：SIGTERM 等5秒再 pkill -9，避免 SQLite WAL 损坏

## 待办
- USDT(TRC20) 发送
- 兑换功能实际对接
- 扫码登录、自绘密码锁、主题切换
- 交易记录分页
- 助记词导入 XHB

## 关键地址
- 归集 TRX：TAqcjs9am3mq9hNZYMZUrfbXoL3fALao6y
- 归集 BNB：0x84d111bC00B315bc6E733730240A363F02F984C9
- GitHub：https://github.com/AHYC1103/XiaoHeiBit
