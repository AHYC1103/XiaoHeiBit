# XiaoHeiBit 历史交接文档

## 项目概述
XiaoHeiBit（小黑币，符号 XHB）是一条从零手写的本地私有区块链，配套纯原生安卓钱包 APP。后端 Python Flask 仅 503KB，全项目不足 1MB。当前版本 v2026.09.24-22。

## 技术栈
- **后端**：Python 3 + Flask，手写 PoW 共识、secp256k1 签名验签、UTXO 模型。chain.db 拆分为 blocks/transfer/nft 三个库（core/__init__.py 中 ATTACH）。主网 8222，测试网 8234。
- **APP**：Kotlin 原生（非 WebView），minSdk 26，arm64-v8a。零 Web 框架，所有 UI 代码手写 LinearLayout。依赖仅 bitcoinj-core 0.16.1（BIP39/BIP44）、zxing（扫码）。
- **加密**：XHB 用手写 secp256k1（零依赖）；多币种用 bitcoinj 派生 + bouncycastle KeccakDigest 做地址编码。
- **公网**：frp 隧道 `cn-hk-bgp-4.ofalias.net:18222`（openfrp），SSH 50044。

## 核心功能
1. **钱包**：创建/导入/导出/重命名/删除，私钥与配置存 APP 私有目录。单币种（XHB 私钥）与多币种（助记词派生）双模式。
2. **多币种**：XHB、TRX、BNB、USDT(TRC20)。BIP44 派生地址已验证与 Gem Wallet 一致。
3. **交易**：
   - XHB 本地签名 → transfer_signed 广播
   - TRX 真实签名广播：r(32)||s(32)||v(1) 65字节 compact，v 在最后，hash=SHA256(raw_data_hex)
   - BNB 真实签名广播：RLP 编码 + EIP-155（chainId=56, v=147+recid）
   - USDT 发送暂未实现（弹"作者没钱测试"提示）
4. **NFT**：列表/详情/铸造/发送/接收，二维码格式 `XiaoHeiBit$Nft$随机码$时间戳`。出售预留。
5. **兑换**：UI 完成，实际链上兑换待对接。
6. **手续费归集**：每笔固定 0.00001，累计达阈值（10 TRX / 0.001 BNB / 5 USDT）自动转 owner 地址，配置从服务端 /api/config 读取。
7. **远程配置**：APP 启动拉取 /api/config，节点列表/汇率/手续费/阈值/归集地址全从服务端读。
8. **引导页**：首次启动显示红底国旗 + 免责声明，点"我已知晓"后不再显示。
9. **网络切换**：公开主链（默认）/ 主网 / 测试网 / 自定义节点。

## 关键修复记录
- **数据库外键 bug**：transactions 表在 transfer schema 下，外键 REFERENCES blocks(height) 解析成 transfer.blocks（不存在），已去掉外键约束。
- **TRX 签名格式**：v在前被拒（Header byte out of range），DER 71字节被拒，最终 r||s||v 65字节 compact 成功。
- **BSC 公钥坐标越界**：toByteArray() 返回33字节（带符号位），截取最后32字节修复。
- **EIP-55 校验和**：BSC 地址加 keccak256 大小写混合。
- **优雅停止**：先 SIGTERM 等5秒，确认死了再 pkill -9，避免 SQLite WAL 没刷盘损坏。
- **红点透明 bug**：颜色 0xEF4444 缺 alpha 通道变成全透明，改成 0xFFEF4444。

## 已知问题与待办
- USDT(TRC20) 发送未实现（trigger smart contract）
- 兑换功能实际对接未做
- 扫码登录（10分钟+设备信息）、自绘密码锁、主题切换预留
- 汇率硬编码，后续可接实时 API
- 交易记录分页未做
- 助记词导入 XHB 未实现（当前 XHB 仅私钥）

## 部署与构建
- 构建：`gradle assembleRelease`，产物 `app/build/outputs/apk/release/app-arm64-v8a-release.apk`
- SSH：`cn-hk-bgp-4.ofalias.net:50044`，用户 `u0_a298`，密码 `Ahyc1103`
- 主链目录：`~/xiaoheibit/`（8222），测试链：`~/xiaoheibit测试/`（8234）
- 公网 API：`http://cn-hk-bgp-4.ofalias.net:18222`
- GitHub：https://github.com/AHYC1103/XiaoHeiBit
- 归集地址：TRX=`TAqcjs9am3mq9hNZYMZUrfbXoL3fALao6y`，BNB=`0x84d111bC00B315bc6E733730240A363F02F984C9`

## 版本号规则
versionCode 递增，versionName = vYYYY.MM.DD-N（当天修改次数）。APK 文件名固定 `app.apk` 覆盖上传。
