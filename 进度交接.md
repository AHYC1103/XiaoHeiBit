# XiaoHeiBit 进度交接（最新）

## 环境
- SSH: u0_a298@cn-hk-bgp-4.ofalias.net -p 50044，密钥 ~/.ssh_xhb/xhb_key
- 服务器目录: ~/xiaoheibit/
- 本地备份: /tmp/api.py /tmp/core.py /tmp/webui.html
- APK编译: ~/jdk-17.0.2 ~/android-sdk ~/gradle-8.3 ~/xhb_app

## 当前状态
- Flask 8222端口正常，高度75，3个钱包
- APK壳在 ~/xiaoheibit/XiaoHeiBit.apk（WebView加载127.0.0.1:8222，摄像头已授权）
- 自动刷新1秒，转账后自动挖矿一块

## 已完成
- 钱包CRUD: 创建(自动命名钱包#N)/导入(私钥)/导出(私钥+二维码)/重命名/换头像(12emoji)/删除
- 收款二维码: XiaoHeiBit$CollectMoney$地址$时间戳
- 导入二维码: XiaoHeiBit$leading-In$私钥$时间戳
- DApp二维码: XiaoHeiBit$DApp$URL$时间戳
- 扫码自动识别三种类型
- 所有原生confirm/prompt换成深色sheet
- jsqr.js外部加载，Flask日志关闭

## 待修
- 导出私钥的二维码画不出来（收款页QR正常，导出sheet里createImgTag不显示）
  - 下次查：sheet打开时机、requestAnimationFrame时机

## API路由
GET: /api/status /api/blocks /api/block/<h> /api/balance/<addr> /api/txs/<addr> /api/mempool /api/wallets /api/validate
POST: /api/wallet/create /switch /delete /rename /import /export /api/transfer /api/mine

## 重启
pkill -9 -f api.py; sleep 2; cd ~/xiaoheibit && nohup python3 api.py > /dev/null 2>&1 &

## 编译APK
export JAVA_HOME=~/jdk-17.0.2 ANDROID_HOME=~/android-sdk
cd ~/xhb_app && ~/gradle-8.3/bin/gradle assembleDebug --no-daemon -Dorg.gradle.jvmargs="-Xmx512m"
